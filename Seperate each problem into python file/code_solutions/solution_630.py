import re

# regex = r'<[^>]+>'
# pattern = re.compile(regex)
# match = pattern.search(html)
# print(match.group())

# regex = r'<[^>]+>'
# pattern = re.compile(regex, re.IGNORECASE)
# match = pattern.search(html)
# print(match.group())

# regex = r'<[^>]