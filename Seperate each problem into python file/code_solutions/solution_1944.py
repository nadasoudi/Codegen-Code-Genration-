def mean(numbers):
    total = 0
    for i in numbers:
        total += i
    return total / len(numbers)

print(mean([1, 2, 3, 4, 5]))

"""

def mean(numbers):
    total = 0
    for i in numbers:
        total += i
    return total / len(numbers)

print(mean([1, 2, 3, 4, 5]))

"""

def mean(numbers):
    total =