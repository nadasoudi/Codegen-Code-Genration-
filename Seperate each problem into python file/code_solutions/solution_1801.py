import numpy as np

# Create a list of 10 random numbers between 0 and 1
np.random.seed(0)
a = np.random.rand(10)

# Create a list of 10 random numbers between 0 and 1
np.random.seed(0)
b = np.random.rand(10)

# Create a list of 10 random numbers between 0 and 1
np.random.seed(0)
c = np.random.rand(10)

# Create a list