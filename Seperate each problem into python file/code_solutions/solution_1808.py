import pandas as pd

# Create a dataframe from the list of tuples
df = pd.DataFrame(list(tuple), columns=['Name', 'Age', 'Job', 'Rating'])

# Print the dataframe
print(df)

# Create a dataframe from the list of tuples
df = pd.DataFrame(list(tuple), columns=['Name', 'Age', 'Job', 'Rating'])

# Print the dataframe
print(df)