import re

def solution(s):
    # Solution 1:
    # s = re.sub(r'\s+','', s)
    # s = re.sub(r'\s+','', s)
    # s = re.sub(r'\s+','', s)
    # s = re.sub(r'\s+','', s)
    # s = re.sub(r'\s+','', s)
    # s