a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

inner_product = np.dot(a, b)

print(inner_product)

"""

import numpy as np

a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

inner_product = np.dot(a, b)

print(inner_product)