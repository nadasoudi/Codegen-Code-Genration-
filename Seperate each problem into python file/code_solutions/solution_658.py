def build_list(seed):
    l = []
    for i in range(seed):
        l.append(i)
    return l

seed = int(input("Enter the seed value: "))
print(build_list(seed))

"""

# Solution

def build_list(seed):
    l = []
    for i in range(seed):
        l.append(i)
    return l

seed = int(input("Enter