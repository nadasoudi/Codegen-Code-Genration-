import re

# regex pattern to match tag(s)
tag_pattern = r'<[^>]+>'

# regex pattern to match other tags
other_tag_pattern = r'<[^>]+>'

# regex pattern to match tag(s)
tag_pattern_2 = r'<[^>]+>'

# regex pattern to match other tags
other_tag_pattern_2 = r'<[^>]+>